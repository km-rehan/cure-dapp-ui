import { UserAppointmentSchedule } from "./UserAppointmentSchedule";
import { connect } from "react-redux";

function mapStateToProps(state) {
    return {
        
    }
}

export default connect(mapStateToProps, {})(UserAppointmentSchedule);