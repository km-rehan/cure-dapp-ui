import { AccountKycStatus } from "./AccountKycStatus";
import { connect } from "react-redux";

function mapStateToProps(state) {
    return {
    }
}

export default connect(mapStateToProps, {})(AccountKycStatus);